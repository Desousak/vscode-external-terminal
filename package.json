{
  "name": "remote-shell-here",
  "displayName": "Remote Shell Here",
  "description": "Open a terminal in the current VS Code folder, including remote workspaces.",
  "version": "1.0.1",
  "publisher": "Desousak",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Desousak/vscode-external-terminal.git"
  },
  "keywords": [
    "macos",
    "terminal",
    "iterm",
    "ssh",
    "remote-ssh"
  ],
  "os": [
    "darwin"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "extensionKind": [
    "ui"
  ],
  "contributes": {
    "commands": [
      {
        "command": "remoteShellHere.open",
        "title": "Remote Shell: Open Here"
      },
      {
        "command": "remoteShellHere.showLogs",
        "title": "Remote Shell: Show Logs"
      }
    ],
    "keybindings": [
      {
        "command": "remoteShellHere.open",
        "key": "cmd+alt+`"
      }
    ],
    "configuration": {
      "title": "Remote Shell Here",
      "properties": {
        "remoteShellHere.app": {
          "type": "string",
          "enum": [
            "terminal",
            "iterm"
          ],
          "default": "terminal",
          "description": "The macOS terminal application to open."
        },
        "remoteShellHere.sshCommand": {
          "type": "string",
          "default": "exec \"${SHELL:-/bin/zsh}\" -l",
          "description": "Command to run on the remote host."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npm run compile && vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
